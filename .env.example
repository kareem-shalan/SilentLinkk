# Do NOT set VITE_API_URL=http://... on Vercel (browser blocks HTTP from HTTPS pages).
# Local and Vercel both use same-origin /api, proxied to http://silentlink.runasp.net.

# Optional server-side override for the Vercel API proxy:
# API_ORIGIN=http://silentlink.runasp.net
