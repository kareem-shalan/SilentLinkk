# Local dev only — optional direct HTTP backend (bypasses Vite /api proxy).
# VITE_API_URL=http://silentlink.runasp.net

# Do NOT set VITE_API_URL=http://... on Vercel.
# Production uses same-origin /api (Vercel rewrite → http://silentlink.runasp.net).
